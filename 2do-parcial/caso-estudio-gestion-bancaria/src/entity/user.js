export class User {
  constructor(username, password, email, nombres, estatus) {
    this.username = username
    this.password = password
    this.email = email
    this.nombres = nombres
    this.estatus = estatus
  }

}
